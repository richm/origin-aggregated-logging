<filter **kibana**>
  @type record_transformer
  enable_ruby
  <record>
    log ${record['err'.freeze] || record['msg'.freeze] || record['MESSAGE'.freeze] || record['log'.freeze]}
  </record>
  remove_keys req,res,msg,name,level,v,pid,err
</filter>
